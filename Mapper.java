import java.util.*;
public class Mapper {
    public static void main(String... args) {
        Map<String, String> mp = new HashMap<>();
        mp.put("tk", null);
        if(mp.get("tk").toString().isEmpty())
            System.out.println(mp.get("tk"));
        String p = null;
      //  p.isEmpty();
        System.out.println(p);


    }
}
